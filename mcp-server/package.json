{
  "name": "wireframe-mapper-mcp",
  "version": "0.1.0",
  "description": "MCP server for AI-driven wireframe generation",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "node build.js && node scripts/verify-build.js",
    "build:only": "node build.js",
    "verify": "node scripts/verify-build.js",
    "start": "node dist/index.js",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@wireframe-mapper/shared": "workspace:*",
    "@modelcontextprotocol/sdk": "^1.0.0",
    "@napi-rs/canvas": "^0.1.83",
    "playwright": "^1.40.0",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "esbuild": "^0.24.0",
    "typescript": "^5.7.0"
  },
  "engines": {
    "node": ">=20"
  }
}
