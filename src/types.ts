/**
 * Type Definitions
 *
 * Re-exports all types from the shared package for backwards compatibility.
 * The actual type definitions live in @wireframe-mapper/shared to be shared
 * between the browser tool and MCP server.
 *
 * @module types
 */

export * from "@wireframe-mapper/shared";
